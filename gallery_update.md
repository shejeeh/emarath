# Gallery Section Update

## Changes Implemented
- **Header Structure**: simplified the gallery header in `index.html`.
  - Removed "Portfolio" label.
  - Changed heading from "Curated Spaces" to "Gallery".
- **Styling**: Added new URL-agnostic styles for `.section-header` in `css/style.css`.
  - Center alignment.
  - Large serif font (`3rem`).
  - Generous bottom spacing (`4rem`) to separate it from the image grid.

## Visual Outcome
- The Gallery section now introduces itself with a clean, centered "Gallery" title, matching the minimal aesthetic requirements.
