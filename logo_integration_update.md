# Logo Integration Update

## Changes Implemented
- **Navbar**: Replaced the text "EMARATH" with an image logo (`assets/logo.png`).
- **Footer**: Replaced the text "EMARATH" with an image logo (`assets/logo.png`).
- **Styling**: 
  - Updated `.logo` in `css/style.css` to align the image correctly in the navbar (height: 40px).
  - Updated `.footer-logo` in `css/style.css` for the footer (height: 60px, margin-bottom: 2rem).

## Action Required
- **Save Logo**: Please save the logo image you provided as **`assets/logo.png`**.
- **Transparent Background**: If possible, use a version of the logo with a transparent background for the best look on the light navbar. If the logo has a solid dark background, it will appear as a block on the light header.
