# Company Name Integration Update

## Changes Implemented
- **Navbar**: Added "EMARATH" text next to the logo image. The logo and text are now displayed side-by-side using flexbox.
- **Footer**: Added "EMARATH" text next to the logo image in the footer, using a new `.footer-brand-lockup` container for alignment.
- **Styling**: 
  - Updated `css/style.css` to style `.logo span` for the navbar (font: Heading font, 1.5rem).
  - Designed `.footer-brand-lockup` and `.footer-logo-text` for the footer (font: Heading font, 1.8rem, white).

## Visual Outcome
- **Top Navigation**: Logo Image (left) + "EMARATH" Text (right).
- **Footer**: Logo Image (left) + "EMARATH" Text (right), followed by the tagline below.
